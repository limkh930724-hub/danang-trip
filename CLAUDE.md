# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 성격

2026년 9월 다낭 가족여행(어른 4명) 일정표. 빌드 도구·패키지 매니저·테스트 프레임워크가 **없다.**
소스는 `index.html` 한 파일(약 1,760줄)이고 나머지는 PWA 부속물(`sw.js`, `manifest.webmanifest`, 아이콘 PNG)뿐이다.

## 실행과 배포

```bash
python -m http.server 8000    # 서비스워커·설치 버튼은 http(s)에서만 동작한다
```

`file://` 로 열어도 화면은 전부 뜨지만 서비스워커 등록과 설치 버튼은 조용히 건너뛴다(`boot()` 에서 프로토콜 검사).
검증은 브라우저에서 눈으로 한다. 린트·테스트 명령이 없다.

배포는 GitHub Pages (`origin` = `limkh930724-hub/danang-trip`). `main` 에 push 하면 그대로 공개된다.
`cf` 리모트는 이 프로젝트와 무관하다.

`sw.js` 를 고치면 `CACHE` 상수(`danang-trip-v1`)의 버전을 올려야 이전 캐시가 비워진다.
전략은 network-first — 일정이 자주 바뀌므로 캐시를 먼저 보면 옛 일정이 뜬다.

## index.html 구조

한 파일 안에서 세 구역이 순서대로 붙어 있다.

| 줄 범위(대략) | 내용 |
|---|---|
| `<style>` (26–774) | CSS 커스텀 프로퍼티 팔레트 + 전체 스타일 |
| `<body>` (776–871) | 정적 껍데기. 헤더, 탭 4개의 빈 `<section>`, 바텀 탭바, 시트/모달/토스트 |
| `<script>` (872–1762) | `PHOTOS` → `TRIP` → 렌더 코드 → 이벤트 위임 → `boot()` |

**데이터와 렌더가 분리돼 있다.** 여행 내용을 바꾸는 일은 거의 전부 `TRIP` 객체(935–1141) 한 군데에서 끝난다.
`days[].items[]` 한 줄의 필드 의미는 README 에 표로 정리돼 있다. `date` 를 고칠 때 `dow`(요일 한 글자)는
자동 계산되지 않으므로 같이 맞춰야 한다.

렌더 함수는 전부 `innerHTML` 문자열 조립이며 프레임워크가 없다. 사용자 입력이나 데이터가 들어가는 자리는
반드시 `esc()` 를 통과시킨다. 이벤트는 개별 바인딩이 아니라 `document` 하나에 건 클릭 위임으로 처리한다
(1632–1701) — 새 동작을 붙일 때도 이 체인에 `closest("[data-*]")` 분기를 추가하는 방식을 따른다.

상태는 전역 `state = { tab, day, placeTab }` 객체 하나. 상태를 바꾼 뒤 해당 `render*()` 를 직접 호출한다.
`localStorage` 키는 `danang.checklist.v1`, `danang.theme.v1` 두 개이며 접근은 모두 `try/catch` 로 감싼다.

## 시간 처리

"지금"의 기준 시간대는 **항상 `Asia/Ho_Chi_Minh`** 이다(`tzNow()`). 헤더 시계만 한국 시간을 같이 보여준다.
`Date` 객체 대신 `"HH:MM"` 문자열을 `toMin()` 으로 분 단위 정수로 바꿔 비교한다.
자정을 넘긴 일정은 `nextDay: true` 를 달면 `+1440` 분으로 계산된다.
1분마다 `setInterval` 로 시계와 일정 탭(`renderBrief`, `renderTimeline`)만 다시 그린다.

## 오프라인 원칙 — 깨뜨리지 말 것

**외부 네트워크 요청이 0건이다.** 폰트·CDN·아이콘 라이브러리·원격 이미지를 절대 추가하지 않는다.

- 사진은 전부 `PHOTOS` 객체에 base64 WebP data URI 로 박혀 있다(파일이 404KB 인 이유. 긴 줄 25개가 이것).
- 헤더 배경 사진도 CSS 변수 `--hero` 안의 data URI 다.
- 아이콘은 인라인 `<svg>`.
- 인터넷이 필요한 유일한 요소는 구글맵 링크(`mapUrl()`)뿐이다.

`PHOTOS` 항목은 위키미디어 커먼즈 CC 라이선스라 저작자 표기가 **법적 의무**다. `by`/`lic` 필드를 지우면 안 된다.
푸터 출처 목록은 `creditList()` 가 `PHOTOS` 를 읽어 자동 생성하므로 사진을 더해도 따로 손볼 곳은 없다.

## 색 규칙

라이트 값은 `:root` 에, 다크 값은 같은 `:root` 안에 `--d-*` 원본 한 벌로 두고
`:root[data-theme="dark"]` 가 매핑만 바꾼다. 색을 고칠 때는 이 한 곳만 손댄다.

- 기기가 다크 모드여도 **밝은 쪽으로 시작**한다. 전환은 헤더 버튼 수동 조작이며 선택은 `localStorage` 에 남는다.
- `--sun`(황동빛)은 **지금 이 순간에만** 쓴다: 현재 시각 선, 진행 중인 일정 카드, 요약 카드 테두리. 이 셋뿐이다.
- 테두리를 두르는 요소도 요약 카드와 진행 중인 일정 둘뿐이다. 나머지는 면과 그림자(낮) / 얇은 선(밤)으로 구분한다.
- 본문·보조 텍스트 대비는 라이트·다크 모두 WCAG AAA(7:1 이상)를 유지한다. 최소 글자 12px, 터치 영역 40px 이상.

## 공개 배포이므로 넣지 않는 값

Pages 로 배포되어 주소를 아는 사람은 누구나 본다. `TRIP.bookings` 에 예약번호·바우처 번호 전체,
예약자 영문 이름, 회원번호, 기사·가이드 개인 휴대폰 번호, 여권/카드/주민등록번호를 적지 않는다.
예약번호는 뒷자리 4자리만 적고 "전체는 단톡방" 형태로 둔다. 호텔 대표번호·총영사관·긴급번호(113/115)는 공개 정보라 무방하다.
실제 값이 필요하면 `.gitignore` 에 등록된 `private/` 에 둔다.

## 커밋

메시지는 한국어 한 줄, prefix 없이 무엇이 바뀌었는지 서술한다 (예: `식당에 베만 씨푸드 추가`,
`설치 버튼이 안 보이고 제목과 겹치던 문제 수정`).
